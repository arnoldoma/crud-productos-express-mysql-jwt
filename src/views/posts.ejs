<%- include('partials/header'); %>

    <div class="container">
        <h1>Listado de productos</h1>
        <div class="posts">
           <% posts.forEach(post => { %>
            <h2> <%= post.title %> </h2>
            <p> <%= post.body %> </p>
         <% }) %>
        </div>
    </div>
    <%- include('partials/footer'); %>